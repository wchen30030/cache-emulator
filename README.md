Description
===========

A cache emulator based on the AMD Opteron Processor measuring cache misses and hits and tested against various optimization metrics. Clone with:

```
git clone https://github.com/wchen30030/cache-emulator
```

Requires the NumPy package.
